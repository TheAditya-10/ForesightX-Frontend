# Frontend Source

This directory contains all application source for the dashboard.

## Structure

- `api/`: backend clients and request functions
- `components/`: reusable UI pieces
- `layout/`: shell-level layout components
- `pages/`: page composition
- `store/`: Zustand state management
- `types/`: frontend contract types
- `utils/`: UI helpers
